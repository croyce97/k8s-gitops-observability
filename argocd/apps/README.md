# Argo CD Applications

Place Argo CD `Application` manifests here. Keep one application per file so changes stay small and easy to review.

Suggested order:

1. `ingress-nginx`
2. `cert-manager`
3. `external-dns`
4. `kube-prometheus-stack`
5. `platform-ingresses`
6. `rancher`
